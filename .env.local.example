# Copy this file to .env.local and paste your real key.
# CONCEPT: .env.local holds secrets. It is git-ignored, so your key
# never ends up on GitHub. The server reads it; the browser never sees it.
ANTHROPIC_API_KEY=sk-ant-your-key-here
